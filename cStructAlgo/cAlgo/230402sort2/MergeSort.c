#include <stdio.h>
#define SIZE 5
int sorted[SIZE];

void merge(int list[], int left, int right)
{
    int mid = (left + right)/2;
    int i, j, k, l;
    i = left;
    j = mid+1;
    k = left;

    while(i<=mid && j<=left)
    {
        if(list[i]>=list[j])
        {
            
        }

        else{

        }
    }
}
